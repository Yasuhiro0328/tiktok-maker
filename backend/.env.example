# バックエンド環境変数
# このファイルを .env にコピーして値を設定してください

# 許可するフロントエンドのオリジン（本番ドメインに変更）
ALLOWED_ORIGIN=https://yourdomain.com

# 一時ファイルの保存ディレクトリ
TEMP_DIR=/var/www/tiktok-tool/temp

# 一時ファイルの保持時間（時間）
FILE_TTL_HOURS=2

# アップロード上限（MB）
MAX_UPLOAD_MB=10
